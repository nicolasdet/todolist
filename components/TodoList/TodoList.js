import { View, Text, StyleSheet } from 'react-native';

const TodoList = () => {
  return (
    <View>
      <Text>List</Text>
    </View>
  );
};

export default TodoList;

const styles = StyleSheet.create({});
